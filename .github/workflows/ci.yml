name: CI
on: [push, pull_request]
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate repo
        run: |
          test -f README.md
          test -f docs/CyberCortex_CTF_PrisonBreak.pdf
